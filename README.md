# random-forest-regression-classification
Machine Learning project implementing Random Forest for both regression and classification tasks with preprocessing, model evaluation, and hyperparameter tuning.
